name: Main github action

on: 
  push:
    branches: [anhnvd]


jobs:
  check_out:
    runs-on: ubuntu-latest

    steps:
      - name: checkout 
        uses: actions/checkout@v4

  setup_java:
    needs: [check_out]
    uses: anhnvdgotrack/CICDTestApplictation/.github/workflows/setup/setup_java.yaml@anhnvd